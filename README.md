# Elevator-Simulator

## TODO
[ ] Use the `State`s evenutally.
[ ] Better elevator algorithm: continues going up until top has been reached,
	then switch states.
[ ] make the buttons the call the elevator, and then upon arriving
	at the elevator, request what floor must be travelled to.
[ ] take a few seconds to go into `DoorOpening` and `DoorClosing` states.
[ ] textures, animations, etc.
